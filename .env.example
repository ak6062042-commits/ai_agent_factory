OPENAI_API_KEY = ...
EXA_API_KEY = ...
DATABASE_URL = ...
MASTER_KEY = ... 

(
    NOTE (for MASTER_KEY): [not needed anymore]
    from cryptography.fernet import Fernet

    # Generate a cryptographically secure random Fernet key
    master_key = Fernet.generate_key()

    # Print the key as a string (keep this secret!)
    print(master_key.decode()) # keep this key as your master_key
)
